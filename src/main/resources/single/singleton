1、总结每种单例写法的优、缺点。
饿汉式
优点：实现方便，没有任何锁，由于在类加载时就已经将对象创建好。所以不存在线程安全问题。
缺点：如果这个类不去使用，它就一直在内存中占用着。不宜大范围使用。

懒汉式
优点：在调用的时候才会去创建对象实例，这样避免了内存空占的问题。
缺点：使用了锁的机制，始终会对性能有一定的影响。

注册式
优点：提供了多个类的单例，比如容器式单例，也是spring中使用的单例模式。适用于创建实例非常多的情况。
缺点：非线程安全

ThreadLocal单例模式
优点：确保了单个线程中线程的安全性。
缺点：

2、思考破坏单例模式的方式有哪些？并且归纳总结。
反射破坏单例
序列化破坏单例
